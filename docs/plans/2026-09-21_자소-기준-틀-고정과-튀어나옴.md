---
project: 한글 폰트 메이커
area: 생성 엔진 ↔ 편집
status: G3 통과 · G1 일부 미측정(레이아웃 변 끌기 · 납작 붓촉 · 추출)
created: 2026-09-21
---

# 자소 기준 틀 고정 — 획을 끌어도 다른 획이 안 움직이고, 상자 밖으로 나갈 수 있게

- **이번에 비교하는 것**: 자소를 상자에 놓을 때마다 "지금 획 전체의 범위"를 다시 재서 꽉 채우는 지금과, 사용자가 처음 고친 순간의 범위를 기준 틀로 굳혀 그 틀을 상자에 맞추는 것.
- **이번 승인으로 바뀌는 것**: 자모 데이터에 `frame`(기준 틀)이 생긴다. 사용자가 고친 자모는 획 하나를 끌어도 나머지가 제자리에 있고, 틀 밖으로 끈 획은 상자 밖으로 튀어나온다. `틀 다시 맞추기`로 되돌린다.
- **바뀌지 않는 것**: 손대지 않은 자모(틀 없음)의 배치 — 프리셋 글자와 OTF 출력은 그대로다. 모델 상자, 레이아웃 Δ, 프리셋 데이터, 추출기 · fitter, production 값.

출처: 9/21 G3 중 사용자 발견("획을 끌다 레이아웃 밖으로 나가면 다른 획 좌표도 같이 바뀐다") + 논의. 튀어나옴은 **형태**다 — 그 자모를 쓰는 모든 글자에 같은 비율로 닿는다. "이 조합에서만"은 레이아웃 오버라이드가 아니라 획 변형(직접 조작 플랜 C의 계열 변형 → 나중의 `이 글자만`)으로 푼다. 순서: 이 플랜 → 표본 줄(B) → 계열 변형(C).

## 지금 상태

**사실**
- 자소 놓기는 `fitNotoComponent`(`src/services/notoComponentFit.ts`) 하나다. `componentBoxFromFaces`가 그 자소 **모든 획의 중심선 범위**(min · max)를 재서 모델의 네 변에 맞는 상자를 만들고, `refineBoxToFaces`가 실제 잉크를 재서 잉크 바깥면이 네 변에 닿도록 상자를 최대 네 번 다듬는다(일자 끝 · 기울어진 끝 보정).
- 그래서 획 하나를 범위 밖으로 옮기면 범위가 커지고 자소 전체가 다시 상자에 맞게 줄어든다. 직접 끌기에서 본 셋(획 하나짜리 ㅁ 통째 옮기기, 상자 끝 점 밀기, ㅘ 줄기의 가로 이동)이 모두 이것이다.
- 이 호출을 화면(문장 · 획 편집), 레이아웃 편집기, 표본 카드, 왼쪽 표지, OTF 추출, 검수가 같이 쓴다(`fitPartStrokes` · `notoComponentFitView`). 프리셋을 만드는 fitter(`skeletonFit` · `centerlineExtract`)도 같은 파일을 쓴다.
- 레이아웃 캔버스의 부품 상자는 "잉크 바깥면"이다(9/21). 상자 변을 끌면 잉크가 같이 온다는 전제가 여기 걸려 있다.
- 계열 변형 자리 `contextStrokes`는 데이터 0건이다.

**가설**
- 틀을 "중심선 좌표 네 값"으로만 저장하면 굳히는 순간 글자가 움직인다. 지금 상자는 잉크를 재서 다듬은 값이고, 그 보정량(끝 모양에 따라 변마다 0 ~ 두께/2)은 상자 크기에 따라 달라져서다. 틀은 "변마다 (획 좌표, 두께 계수)"여야 모든 문맥에서 굳히기 전과 같은 상자가 나온다.

## 하려는 것

1. `JamoData.frame` — 변 넷에 (획 좌표, 두께 계수). 있으면 `fitNotoComponent`가 획 범위 · 잉크 재기 대신 이 틀로 상자를 푼다. 없으면 지금 그대로.
2. 굳히는 때: 사용자가 그 자모를 처음 고치는 기록에서, **고치기 전** 획으로 틀을 재서 같이 저장한다. 끄는 중 미리보기도 같은 틀을 쓴다 — 끄는 동안부터 다른 획이 안 움직인다. 되돌리기(Undo · `뒤로`)는 틀도 같이 되돌린다.
3. 튀어나옴은 계산값이다. 획 잉크가 틀보다 크면 튀어나온 것이고, 어느 변 · 몇 u인지도 거기서 나온다. 플래그를 저장하지 않는다(검수 필터 · 숫자 칩이 나중에 이 계산을 쓴다).
4. `틀 다시 맞추기` — 도구 줄 위 상태 줄 옆. 틀을 지워 지금 획으로 다시 꽉 채운다(기록 한 줄). 틀이 있을 때만 보인다.
5. 보여 주기: 획 캔버스와 레이아웃 캔버스의 부품 상자는 틀의 바깥면이다. 튀어나온 잉크는 상자 밖에 그대로 그린다(자르지 않음). 레이아웃에서 상자 변을 끌면 틀이 움직이고 튀어나온 양은 비율로 따라온다.
6. 부딪힘: 튀어나온 획이 옆 자소에 닿는 것은 지금의 최소 잉크 간격 멈춤이 막는다. 문장에 없는 글자에서의 부딪힘은 이 플랜에서 안 본다.

## 게이트

| 게이트 | 닫는 조건 |
|---|---|
| D0 계약 | 틀의 꼴(변마다 좌표 + 두께 계수)과 상자를 푸는 식. **굳히는 순간 여섯 레이아웃 모두에서 상자가 안 바뀐다**(오차 한도 1u). 전역 두께 · 붓촉을 바꿔도 틀이 유효한지(안 되면 그때 다시 잰다고 명시). 저장 형식 검증(`fontDataPayloadValidation`) · 옛 저장분은 틀 없음으로 읽힘. 계열 변형이 생기면 변형마다 틀 하나. |
| G0 대표 | `각` ㄱ: 세로 획 끝점을 상자 아래로 60u 끌기 → 가로 획 좌표 그대로, 끝점은 손가락 자리, 상자 밖으로 나옴. `가` · `고` · `과` · `곡`에서도 같은 비율로 나옴. ㅁ 통째 옮기기와 ㅘ 줄기 가로 이동이 이제 움직임. `틀 다시 맞추기` → 꽉 참. Undo가 틀까지 되돌림. Playwright. |
| G1 홀드아웃 | 굳히기 전후 상자 비교를 자모 67개 × 여섯 칸 전수로(단위 테스트). 안쪽으로 줄이기(빈 자리 생김), 받침 · 혼합 홀자, 납작 붓촉, 레이아웃에서 상자 변 끌기(틀이 움직임), OTF 추출에 같은 튀어나옴. |
| G2 전수 | 틀 없는 자모 전부에서 상자가 비트 단위로 같음(11,172자 표본 해시 또는 전수). `tsc -b` · `npm test` · e2e `workspace-jamo-*` · 추출 관련 통과. 화면 명세 고침. |
| G3 사용자 검증 | 사용자가 직접 끌어 "다른 획이 안 움직인다 · 튀어나온다"를 판정. 직접 조작 플랜 A의 G3도 이 위에서 같이 본다. |

## 미결정

없음. 셋 다 9/21에 추천안으로 닫혔다(처음 고칠 때 자동 · 튀어나온 잉크는 그대로 그림 · 두께는 따라감).

## 이 플랜에 없는 것

- 검수 탭의 튀어나옴 필터와 오버라이드 숫자 칩(`전체` 층은 안 셈, `레이아웃` · `획` 구분) — 검수 격자 플랜의 다음 조각.
- 계열 변형 · `이 글자만`(직접 조작 플랜 C와 그 뒤), 프리셋에 틀을 미리 넣는 일.
- 문장 밖 글자와의 부딪힘 전수 검사.

## 진행 기록

**2026-09-21** 플랜 작성. 구현 없음.
- 사실: 위 `지금 상태` 다섯 줄(코드 확인).
- 가설: 위 하나. 식은 D0에서 풀고 전수 테스트로 확인한다.
- 미결정: 위 셋.
- 논의에서 정한 것: 튀어나옴은 허용한다(아티스틱한 글꼴). 조합별 레이아웃 오버라이드로 풀지 않는다 — 획은 비율로 공유돼서 한 글자의 상자만 키우면 나머지 글자의 그 자모가 찌그러진다.

**2026-09-21 승인 · 구현** 사용자: 미결정 셋 추천안 ✔, `구현해`.

사실
- **D0에서 틀의 꼴을 바꿨다.** 플랜의 "변마다 좌표 + 두께 계수"가 아니라 **고치기 전 획의 사본**(`JamoData.frame` = 획 채널 사본, `src/utils/jamoFrame.ts`)이다. `fitNotoComponent`는 틀이 있으면 상자 계산(범위 맞춤 + 잉크 재서 다듬기)을 사본으로 하고 지금 획은 그 상자에 그대로 놓는다. 같은 계산을 사본에 돌리는 것이라 굳히는 순간의 상자는 어느 문맥 · 두께 · 붓촉에서도 정의상 같다 — 계수식의 근사 오차(기울어진 획 · 곡선)가 없고, 두께를 바꾸면 사본의 잉크를 다시 재므로 저절로 따라간다(미결정 3).
- 굳히는 때: 기록을 남기는 한 곳(`commitJamo`)과 미리보기 한 곳(`previewJamoWithContextSafety`)에서 `frameForEdit` — 저장돼 있던(고치기 전) 획으로 굳힌다. 끄는 중의 간격 계산도 같은 틀을 본다. 기록의 `before`에는 틀이 없어서 Undo · `뒤로`가 틀까지 되돌린다.
- `틀 다시 맞추기`(`jamo-frame-reset`): 틀 없이 저장하는 유일한 길(`{ unframed: true }`). 틀 상태 줄은 늘 자리를 차지해 굳는 순간 도구 줄이 안 밀린다(고정 높이 274 → 306).
- 튀어나옴 = `componentProtrusion`(틀 사본과 지금 획의 중심선 범위 차 × 상자, em). 저장 안 함. 상태 줄에 `상자 밖 아래 +57u`.
- 저장 형식: `fontDataPayloadValidation`이 `frame`을 받는다(채널 넷, 획 배열 검증). 옛 저장분은 틀 없음.
- G0(Playwright): `각` ㄱ 세로 획 끝점을 아래로 30px → 끄는 중에도 나머지 두 점 좌표 그대로, 끝점은 손가락 자리, `상자 밖 아래 +57u`. `고`에서도 같은 ㄱ이 비율대로 나옴(+46u, 상자가 낮아서). `틀 다시 맞추기` → 꽉 참 · 상태 줄 빔, Undo → 튀어나온 채로 복귀.
- G1: 단위 `jamoFrame.test.ts` — 자모 전부(초성 · 받침 · 홀자 채널별, 60개 넘음) × 상자 꼴 넷 × 계열 셋에서 굳히기 전후 상자가 **완전히 같음**(500건 넘게 비교). 틀이 있으면 끝점을 0.3 밀어도 상자 그대로 · 없으면 줄어듦, 튀어나온 양, 틀은 한 번만 굳음.
- G2: 내 파일 `tsc` · eslint 0. 단위는 fit · 틀 · 칸 해석 · 추출 쪽 전부 통과. e2e는 새 테스트(`획을 상자 밖으로 끌어도 …`) 통과.
- **못 돌린 것**: 레이아웃 모드를 지나는 e2e와 `layout-delta-store` · `layout-overrides` 단위 12개. 다른 세션이 `layoutDeltaStore` · `scopeRule`을 고치는 중이라 워킹트리에서 레이아웃 모드가 안 뜬다(`tsc` 오류 4건도 그쪽). 그래서 "레이아웃에서 상자 변을 끌면 틀이 움직인다" · 납작 붓촉 · OTF 추출의 튀어나옴은 **안 쟀다**.

가설
- 레이아웃 캔버스 · 표본 카드 · 추출은 같은 `fitPartStrokes`를 써서 코드상 저절로 따라온다. 위 사정으로 눈으로는 못 봤다.

발견(이 플랜 밖)
- 최소 잉크 간격 멈춤이 셸 안에서는 믿을 만하지 않다. 간격을 재는 문맥(`collisionContexts`)이 옛 스키마 상자(`calculateBoxes`)로 글자를 놓는데 화면은 모델 상자로 그린다. `각`에서 ㄱ 다리를 받침 위까지 끌어도 안 멈췄다. 하려는 것 6번("지금의 멈춤이 막는다")은 **틀렸다**. 따로 고쳐야 한다.

**2026-09-21 G3 닫힘** 사용자가 직접 끌어 보고 통과("좋당 · 직접 해봄"). 남은 것은 위 `못 돌린 것`(레이아웃 변 끌기 · 납작 붓촉 · 추출)과 최소 잉크 간격 멈춤 — 간격 멈춤은 다음 플랜.
