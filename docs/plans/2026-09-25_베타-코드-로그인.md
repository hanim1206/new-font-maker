---
project: 한글 폰트 메이커
area: 공통 기반
status: G0 구현 (실제 Supabase 확인 대기)
created: 2026-09-25
출처: 오픈 전 리스트 24 · 대화 2026-09-25(초대코드, 이메일 없이)
---

# 베타 코드 로그인

- **이번에 비교하는 것**: 친구가 이메일 없이 **카톡으로 받은 코드 하나만 넣고** 들어오는 로그인이 헷갈림 없이 되는지.
- **이번 승인으로 바뀌는 것**: 앱을 열면 로그인 안 된 사람은 코드 입력 화면을 먼저 본다. 계정은 내가 스크립트로 미리 만들고, 스스로 가입은 막는다.
- **바뀌지 않는 것**: 편집 데이터를 저장하는 곳(아직 브라우저. 계정 저장은 26번 플랜), 편집 화면, 추출, 개발 서버에서 Supabase 설정이 없을 때의 동작(로그인 없이 그대로).

9/24 결정 "계정은 미리 만들어 나눠 주고 스스로 가입은 막는다"(24번)와 같은 방향이다. 뒤집는 결정은 없다.

## 지금 상태 (사실, 코드 조사)

- Supabase 이메일·비번 로그인이 `src/stores/authStore.ts`에 있지만, 부르는 곳은 죽은 옛 편집기(`src/components/AuthForm.tsx` 등)뿐이다. `src-next`에는 로그인이 없다.
- `font_projects` 테이블의 `user_id`는 빈 값이 허용된다(`src/types/database.ts:80`). 로그인 안 한 상태의 `list()`는 걸러 내지 않고 전부 조회한다(`fontProjectService.ts`). 행 단위 권한(RLS)이 켜져 있는지는 레포에 마이그레이션이 없어 모른다.
- 레포 마이그레이션은 버전 되돌림을 막는 트리거 하나뿐이다.

## 계약 (D0 안)

- **코드 모양**: `K7QM-4XPA-9TRD`처럼 4자 세 묶음. 헷갈리는 글자(0 O 1 I L)는 뺀다. 입력할 때 대소문자, 하이픈, 공백은 봐준다.
- **코드 → 계정**: 앞 묶음이 계정 이름, 코드 전체가 비번이다. 앱은 `k7qm@beta.<도메인>`으로 로그인한다. 코드와 이메일을 잇는 표가 필요 없다. 친구는 이메일을 보지 않는다.
- **계정 만들기**: 로컬에서만 도는 `scripts/beta-accounts.mjs`. `service_role` 키로 `admin.createUser`(메일 인증 완료로)를 호출하고, `이름 · 코드` 목록을 파일로 떨군다. 이 키와 목록은 커밋하지 않는다(`.gitignore`). `VITE_` 환경변수에 넣지 않는다.
- **가입 막기**: Supabase 대시보드에서 `Allow new users to sign up`을 끈다. 앱에는 가입 화면이 없다.
- **권한**: `font_projects`에 RLS를 켜고, 본인 행만 읽고 쓰는 정책을 마이그레이션으로 둔다(26번이 저장을 붙이기 전에 먼저).
- **로그인 유지**: Supabase 기본값(브라우저에 세션을 두고 자동 갱신). 로그아웃은 메뉴 깊은 곳에만 둔다.
- **코드를 잊으면**: 카톡으로 나에게 묻는다. 같은 계정에 새 코드를 발급하면(비번 교체) 폰트는 그대로 남는다.
- **화면**: 제품 주소 전체 앞에 게이트. 로그인 안 됐으면 코드 입력칸 하나 + `들어가기`. 틀리면 "코드를 다시 확인해 주세요" 한 줄.

## 게이트

| 게이트 | 닫는 조건 |
|---|---|
| D0 계약 | 위 계약 + 아래 미결정 셋 답. 대시보드에서 RLS 상태와 가입 설정을 실제로 확인해 적는다. |
| G0 대표 | 스크립트로 계정 둘 생성. 코드 A로 들어가기 · 틀린 코드 막힘 · 새로고침해도 유지. 스크린샷. |
| G1 홀드아웃 | 다른 기기(폰)에서 같은 코드. 소문자·공백 섞은 입력. 로그아웃 뒤 재로그인. 새 코드 재발급 뒤 옛 코드 막힘. 계정 A로 B의 행 조회 → 0건. |
| G2 전수 | 친구 수만큼 생성. e2e(로그인 게이트 · 틀린 코드), `npm test`, production 빌드에 `service_role` 문자열 0건. 전체 `npm test` 1039 통과 · 12 실패 — 12개는 이 변경 없이도 똑같이 실패한다(이 컨테이너에 Noto 코퍼스가 없는 것, 이미 적힌 digest · HTML 해시, `/global-style-preview` 주소가 목록에 없는 것). |
| G3 사용자 검증 | 친구 한 명이 카톡으로 받은 코드만으로 설명 없이 들어오는지. |

## 결정 (2026-09-25 사용자)

1. **이메일 도메인**: 가짜 도메인 `beta.fontmaker.invalid`(`VITE_BETA_EMAIL_DOMAIN`으로 바꿀 수 있음). 친구 닉네임은 계정 메타데이터에 둔다 — 코드만 넣는 로그인에서 앱이 닉네임을 알 수 없어서 이메일 앞자리는 코드 앞 네 글자로 둔다.
2. **로그인 전 화면**: 이 사이트가 무엇인지 알려 주는 about 페이지를 따로 만든다(이 플랜 밖). 그전까지 코드 입력 화면에 한 줄 소개.
3. **메시지**: 링크 · 코드 · 설치 안내 · 제보 안내를 카톡 한 통으로.

## 이 플랜에 없는 것

- 편집 데이터의 계정 저장, 한 계정 폰트 1개, 폐기·보관함(26 · 24번).
- 카카오 로그인(한글날). 그때 공개 가입을 열면 "코드가 있어야 가입"으로 바꾸는 건 별도 플랜.
- about 페이지. 만들면 로그인 게이트 앞(`main.tsx`의 `gate()`)에서 그 주소만 통과시킨다.
- 메인 화면(01번). 로그인 뒤 어디로 갈지는 01이 정한다. 그전까지는 지금처럼 `/workspace/jamo`.

## 진행 기록

**2026-09-25** 플랜 작성. 구현 없음.
- 사실: 위 `지금 상태` 세 줄.
- 가설: 코드 앞 묶음을 계정 이름으로 쓰면 코드와 이메일을 잇는 표 없이 로그인할 수 있다. 4자 세 묶음(31자 알파벳, 약 59비트)이면 Supabase 로그인 횟수 제한과 합쳐 친구 베타에는 충분하다.
- 미결정: 위 셋. 게이트가 닫히면 옵시디언 `결정과 뒤집힘.md`에 한 줄. 이 세션은 볼트에 접근할 수 없어 맥 세션에서 남긴다.

**2026-09-25 결정 · 구현** 사용자 `구현해`. 결정 셋은 위 `결정` 절.
- 사실(코드): `src-next/betaCode.ts`(코드 모양 · 코드 → 로그인 값 · 코드 생성 · 초대 메시지), `betaAuth.ts`(게이트 on/off/misconfigured, 로그인 오류 네 갈래), `BetaLoginPage.tsx`(입력칸 하나), `main.tsx`의 `gate()`가 랩 포함 모든 주소 앞에서 세션을 본다. 셸 `☰` 메뉴 맨 끝에 `로그아웃`(게이트가 켜졌을 때만).
- 사실(게이트 규칙): Supabase 설정이 있으면 켠다. `VITE_AUTH_GATE=off`면 끈다(playwright `webServer`에 넣음). 개발 서버에 설정이 없으면 끈다. **배포 빌드에 설정이 없으면 앱을 열지 않는다** — 지금 배포에 Supabase 환경변수가 없으면 이 커밋부터 막힌다.
- 사실(스크립트): `npm run beta:accounts -- add|reissue|list <닉네임>`. `.env`의 `SUPABASE_SERVICE_ROLE_KEY`로 계정을 만들고 `beta-accounts/`(gitignore)에 친구별 카톡 메시지를 쓴다. 중간에 실패해도 이미 만든 코드는 파일에 남는다.
- 사실(마이그레이션): `20260925120000_font_projects_owner_rls.sql` — RLS 켜고 본인 행만. 아직 Supabase에 적용하지 않았다.
- 사실(검증): 단위 테스트 10개(`beta-code` 6 · `beta-auth` 4), `tsc -b`, 새 파일 eslint 통과. 브라우저: 없는 Supabase 주소로 게이트를 켜서 로그인 화면 · 모양 틀림 · 연결 실패 문구 확인, Supabase 응답을 가로채 성공 흐름(보낸 값 `k7qm@… / K7QM-4XPA-9TRD`, 새로고침 뒤 유지, 로그아웃하면 코드 화면) 확인. production 빌드에 `service_role` 문자열 0건. 전체 `npm test` 1039 통과 · 12 실패 — 12개는 이 변경 없이도 똑같이 실패한다(이 컨테이너에 Noto 코퍼스가 없는 것, 이미 적힌 digest · HTML 해시, `/global-style-preview` 주소가 목록에 없는 것).
- 가설: 가짜 도메인 `.invalid`를 Supabase 관리자 API가 받아 준다. 실제 프로젝트에서 `add` 한 번으로 확인해야 한다.
- 미결정(G0을 닫으려면 사용자가 할 일): ① Supabase 대시보드에서 `Allow new users to sign up` 끄기 ② RLS 마이그레이션 적용 ③ `.env`에 `SUPABASE_SERVICE_ROLE_KEY` · `BETA_APP_URL` 넣고 `add` 둘로 실제 로그인 확인 ④ 배포 환경변수 확인. 게이트가 닫히면 옵시디언 `결정과 뒤집힘.md`에 한 줄(맥 세션).

**2026-09-25 RLS 적용** 계획의 dev가 아니라 사용자 결정으로 main Supabase에 SQL Editor로 직접 적용(`db push` 안 씀, 이 파일 하나만).
- 사실: `font_projects_*_own` 정책 넷이 생겼다. main에는 이미 `select_own · insert_own · update_own · delete_own`(역할 `public`, `auth.uid() = user_id`)이 있었다 — 효과가 같아 그대로 둔다. `enable row level security` 실행 성공. `user_id`가 빈 옛 행 2개(지우지 않음).
- 미결정: 옛 정책 넷이 있었으니 RLS가 원래 켜져 있었을 수 있다(적용 전 값은 확인 안 함). 옛 정책 넷 정리 여부. 미결정 ②는 닫힘, ①③④는 남음.

**2026-09-25 시험 계정 · 로그인 확인** main Supabase.
- 사실: `.env`에 `SUPABASE_SERVICE_ROLE_KEY` · `BETA_APP_URL=https://hangul-mus.vercel.app`. `add test1 test2`로 계정 둘 생성 — 가설 "`.invalid` 도메인을 관리자 API가 받아 준다"는 맞았다. 로컬 `npm run dev`에서 코드로 로그인 확인(사용자).
- 사실: 지금 앱(`src-next`)은 `font_projects`를 쓰지 않는다(`fontProjectService`를 가져다 쓰는 곳 없음). RLS 적용이 운영 앱을 막는다는 앞의 걱정은 틀렸다.
- 사실: 로그인 코드(`7eddd0b`)는 아직 `main` · `dev`에 없다. 운영 배포는 `fa06b0a`.
- 미결정: ① 가입 끄기 ④ Vercel Production의 `VITE_SUPABASE_URL` · `VITE_SUPABASE_ANON_KEY` 확인 → 그다음 main에 합칠지. service role 키는 대화에 노출돼 나중에 교체(사용자 결정), 교체하면 `.env`도 바꾼다.

**2026-09-25 main 배포** 사용자가 `92f8179`까지 main에 합쳐 배포했다.
- 사실: 운영(`hangul-mus.vercel.app`) 새 빌드 `index-fzxo4omK.js`에 `BetaLoginPage` 청크가 있다. 모든 JS 청크에서 `service_role` 0건. 번들에 Supabase 주소가 들어 있어 Production 환경변수가 있다 — 미결정 ④ 닫힘.
- 사실(사용자): Supabase에서 `Allow new users to sign up`을 껐다 — 미결정 ① 닫힘.
- 미결정: 운영 주소에서 `test1` 코드로 로그인 확인(사용자) → G0 닫힘. service role 키 교체.
