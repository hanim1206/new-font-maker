---
project: 한글 폰트 메이커
area: 공통 기반
status: 진행 중 (G2 닫힘, G3 사용자 검증 대기)
created: 2026-09-20
---

# 화면별 기능 명세 트리

- **이번에 비교하는 것**: 기능 명세 한 줄의 굵기. "ㅇㅇ페이지 ㅇㅇ기능"이라고 짚을 수 있을 만큼 잘게, 한눈에 들어올 만큼 짧게 — 아래 예시 형식이 그 균형에 맞는지.
- **이번 승인으로 바뀌는 것**: `docs/specs/화면/`에 화면당 파일 하나씩 명세가 생긴다. 형식이 확정된 뒤에 개발 서버 전용 플로팅 버튼이 지금 경로의 명세를 띄운다.
- **바뀌지 않는 것**: 화면 동작, 라우트, 저장 데이터, production 빌드(버튼은 dev에서만), 기존 `UX·화면 명세`(설계 문서로 그대로 둠), `원칙.md`.

출처: 주간 노트 2026-W39 일요일 `아이디어`.

## 지금 상태

**사실** (코드 읽기로 확인)
- 라우트는 `window.location.pathname` 분기로 흩어져 있다. 제품 화면 7개: `/` 문장 보정, `/workspace/jamos` 자소 현황, `/workspace/jamo` 자소 편집, `/workspace/jamo/master` 원형, `/workspace/jamo/result` 조합별 결과, `/workspace/skeleton` 뼈대, `/workspace/review` 검수 격자. 랩 9개(`/grid-lab` `/rule-lab` `/font-guide-lab` 등).
- `docs/specs/KOREAN_FONT_MAKER_UX_SCREEN_SPEC.md`(1,211줄)는 "이렇게 만들자"는 설계다. 화면 ID(P-01, J-02…)가 있지만 지금 코드의 화면과 1:1이 아니다. 지금 있는 기능을 적은 문서는 없다.
- 자소 편집·검수 화면은 `레이아웃 수정을 자소 탭으로 옮기기`로 9/20에 바뀌었다(자소 탭 `획 · 레이아웃` 전환, 검수는 격자만, 미커밋 · G3 대기). G0 명세는 이 상태를 기준으로 쓴다.

## 하려는 것

1. **명세는 지금 있는 것만 적는다.** 만들 것은 플랜에, 있는 것은 명세에. 폐기 예정 화면도 있는 그대로 적고 `폐기 예정`만 표시한다.
2. 화면당 파일 하나, `화면 → 영역 → 기능` 세 단. 기능은 한 줄: 무엇을 하는지 · 어디에 저장되는지. 기능마다 번호를 붙여 `자소편집 2.3`처럼 부른다.
3. `docs/specs/화면/_트리.md`에 전체 화면 목록과 서로 넘어가는 길을 한 장으로 둔다.
4. 형식이 G0에서 확정되면 dev 전용 플로팅 버튼을 붙인다. 파일 머리말의 `route`로 지금 경로의 명세를 찾아 그대로 보여 준다. 명세를 두 번 쓰지 않는다.
5. 화면을 고치는 플랜은 닫을 때 해당 명세 파일도 같이 고친다(진행 기록에 한 줄).

형식 예시(내용은 자리 표시, G0에서 실제로 채움):

```markdown
---
route: /workspace/jamo
name: 자소편집
status: 사용 중
---
# 자소 편집
문장 속 글자 하나를 골라 그 글자의 자소를 고친다.

## 1. 문장 줄
- 1.1 글자 고르기 — 누른 글자가 편집 대상이 된다 · 주소의 `char`
- 1.2 …

## 2. 편집 캔버스
- 2.1 획 앵커 옮기기 — … · 자모 스토어
```

## 게이트

| 게이트 | 닫는 조건 |
|---|---|
| D0 계약 | 둘 곳, 머리말 항목(`route` `name` `status`), 세 단 구조, 번호 규칙, "있는 것만 적는다" 확정. |
| G0 대표 | 자소 편집 · 검수 격자 두 화면을 코드 기준으로 작성. 사용자가 읽고 **한 줄의 굵기**를 판정(너무 잘다 / 맞다 / 너무 굵다). 판단 변수는 이것 하나. |
| G1 홀드아웃 | 성격이 다른 화면 둘(문장 보정, 랩 하나)을 같은 형식으로 작성. 형식을 안 고치고 들어가는지 본다. |
| G2 전수 | 범위 안 모든 화면 작성 + `_트리.md`. 라우트 목록과 명세 파일의 `route`가 1:1인지 보는 테스트 추가, `npm test` 통과. 플로팅 버튼 구현, production 빌드에 안 들어감을 확인, Playwright로 토글 확인. |
| G3 사용자 검증 | 사용자가 트리만 보고 기능 셋을 번호로 짚어 대화가 되는지, 개발 화면에서 버튼으로 같은 내용이 뜨는지 판정. |

## 미결정 (승인 때 같이 답해 주세요)

1. **둘 곳** — 레포 `docs/specs/화면/`에 두고 볼트에 심볼릭 링크로 걸어 옵시디언에서 읽기(추천: 앱이 바로 읽고 문서 규칙 그대로) vs 옵시디언에 직접(규칙에 예외 추가, 앱이 읽으려면 복사 단계 필요).
2. **범위** — 제품 화면 7개 먼저(추천) vs 랩 9개까지. 랩은 G1에서 하나만 형식 확인용으로 쓴다.
3. **버튼이 보여 줄 것** — 지금 화면의 명세 전체(추천: 단순, 명세 그대로) vs 화면 한 줄 설명만.

## 이 플랜에 없는 것

- 기존 `UX·화면 명세` 정리나 화면 ID 통일.
- 흩어진 라우트 분기를 한 곳으로 모으는 리팩터(테스트에 필요한 목록 상수까지만).
- 화면 자체의 변경. 명세를 쓰다 발견한 문제는 옵시디언 `열린 질문`거리로만 알린다.

## 진행 기록

**2026-09-20** 플랜 작성. 구현·명세 작성 없음.
- 사실: 위 `지금 상태` 세 줄.
- 가설: 세 단이면 모든 화면이 들어간다(랩처럼 패널이 많은 화면은 G1에서 확인). Vite `import.meta.glob` + `import.meta.env.DEV`로 명세를 dev에서만 읽을 수 있다. 옵시디언이 심볼릭 링크 폴더를 문제없이 읽는지는 D0에서 확인.
- 미결정: 위 셋.

**2026-09-20 승인 · D0 닫힘** 구현·명세 작성은 아직 없음(`구현해` 대기).
- 사실: 사용자 결정 셋 — ① 레포 `docs/specs/화면/` + 볼트 심볼릭 링크 ② 제품 화면 7개 먼저(랩은 G1에서 하나만) ③ 버튼은 지금 화면의 명세 전체를 띄움. 형식 계약(머리말 `route` `name` `status`, 세 단, 기능 번호, 있는 것만 적기)은 플랜 본문 그대로 확정. `결정과 뒤집힘.md`에 한 줄 남김.
- 가설: 옵시디언이 심볼릭 링크 폴더를 읽는다(공식 지원이지만 볼트 밖 변경 감지가 늦을 수 있음). G0에서 링크를 걸 때 실제로 열어 확인한다.
- 미결정: 없음. 다음은 G0 — 자소 편집 · 검수 격자 두 화면 작성, 판단 변수는 한 줄의 굵기 하나.

**2026-09-20 G0 작성** `구현해` 받음. 코드 변경 없음.
- 사실: `docs/specs/화면/자소편집.md`(8영역 48줄)와 `검수격자.md`(5영역 15줄)를 코드 기준으로 작성. 읽은 파일: `CalibrationSentenceEditor` `GlyphLayoutEditor` `ReviewPropagationCards` `ReviewWorkspacePage` `WorkspaceChrome` `NotoCorpusMatrix` `BrushStyleTrackpad` `ShapeRulePanel`. 화면을 띄워 확인하지는 않았다.
- 사실: 볼트 `개인 프로젝트/폰트메이커/화면 명세` → 레포 `docs/specs/화면` 심볼릭 링크를 걸었다. 셸에서는 두 파일이 보인다. 옵시디언 앱에서 열리는지는 사용자 확인 필요.
- 사실: 머리말에 `updated`(날짜)를 하나 더했다. 명세가 낡았는지 보려는 것. D0 계약에 없던 항목이라 G0 판정 때 같이 본다.
- 쓰다가 발견한 것(화면은 안 고침): ① 형태 규칙 패널에 적는 부리·예절은 `localStorage` 메모로만 남고 글자에 안 닿는다. ② 자모 형태 Δ(`이 자모로 올리기`)는 미리보기뿐 저장이 없다(레이아웃 플랜의 미결정과 같음). ③ 셸의 홈·출력·더보기 버튼은 꺼져 있다. ④ 문장 줄의 문장은 저장되지 않는다.
- 가설: 자소 편집 48줄이 한 화면 최대치에 가깝다. 다른 화면은 이보다 짧을 것.
- 미결정: 한 줄의 굵기 판정(너무 잘다 / 맞다 / 너무 굵다). 판정 전에는 G1으로 가지 않는다.

**2026-09-20 G0 닫힘 · G1 작성** 코드 변경 없음.
- 사실: 사용자 판정 — 한 줄의 굵기 `맞다`, 머리말 `updated` `둔다`. 옵시디언에서 심볼릭 링크 폴더가 열리는 것도 사용자가 확인. `결정과 뒤집힘.md`에 한 줄 남김.
- 사실: G1로 `문장보정.md`(`/`, 5영역)와 `형태그리드랩.md`(`/grid-lab`, 4영역 16줄)를 작성. 세 단 · 번호 · 머리말은 안 고치고 들어갔다. `status`에 `실험` 값을 하나 썼다(랩용).
- 사실: 문장 보정은 자소 편집과 같은 편집기(`chrome`만 다름)라 같은 영역은 다시 안 쓰고 `자소편집 3과 같음`처럼 번호로 가리키고 다른 점만 적었다. 두 번 쓰면 어긋나서다. 대신 그 화면 명세만 봐서는 전부 안 보인다 — G2에서 버튼이 가리킨 영역을 펼쳐 보여 줄지 정해야 한다.
- 쓰다가 발견한 것(화면은 안 고침): ① `/`에서는 자소를 통째로 옮기면 옛 스키마에 저장되는데, `Noto 배치`가 켜져 있으면 글자에 안 닿는다. 자소 탭에서 막은 문제가 `/`엔 그대로다(레이아웃 플랜의 미결정 `/`에도 전환을 둘지와 같은 건). ② `/grid-lab`의 `보정 화면` 링크는 없는 주소 `/editor-v2`로 가는데, 모르는 주소가 전부 `/`로 떨어져서 우연히 동작한다. ③ 형태 그리드 데이터는 앱 어디서도 읽지 않는다(`gridV1Import`는 테스트만 부른다).
- 가설: 같은 편집기를 쓰는 화면이 더 나오면(원형 · 조합별 결과) 같은 방식으로 가리키면 된다.
- 미결정: G1 확인 — "다른 점만 적고 번호로 가리키기"가 읽기에 괜찮은지. 괜찮으면 G2(나머지 4화면 + `_트리.md` + 테스트 + 플로팅 버튼).

**2026-09-21 G1 닫힘 · G2 구현** 사용자 `진행해`.
- 사실(명세): 남은 네 화면 `뼈대.md` `자소현황.md` `자소원형.md` `조합별결과.md`와 `_트리.md`(화면 트리 · 넘어가는 길 · 글자에 닿는 저장소 표)를 작성. 제품 화면 7개 + 랩 1개. 네 화면의 기능 목록은 서브에이전트 셋이 코드를 읽어 뽑았고, 글자에 닿는지에 대한 핵심 주장(`resolveAllGridBoundSchemas` 사용처 한 곳, `useShapeSystemStore`를 읽는 화면은 `ShapeWorkspacePage`뿐)은 직접 grep으로 확인했다. 화면을 띄워 하나씩 눌러 보지는 않았다.
- 사실(코드): `src-next/screenSpec.ts`(읽기 · 가리킨 번호 풀기), `screenRoutes.ts`(주소 목록), `ScreenSpecButton.tsx` + CSS, `mountScreenSpecButton.tsx`, `main.tsx`에 `import.meta.env.DEV` 분기 한 줄. 버튼은 앱과 따로 떨어진 루트에 붙어 랩 포함 모든 주소에서 뜬다. 좁은 화면은 왼쪽 가장자리 얇은 탭, 640px 이상은 오른쪽 아래 구석 — 처음엔 오른쪽 아래 고정이었는데 좁은 화면에서 트랙패드를 가려서 바꿨다. 열림 상태는 이 기기에 기억, Esc로 닫힘. 가리킨 줄(`자소편집 3`)은 그 자리에서 펼쳐진다.
- 사실(검증): `screen-spec.test.ts` 15개 통과 — 제품 주소마다 명세 하나, 명세 주소는 목록 안, 라우팅 코드의 주소 글자는 전부 목록 셋 중 하나, 영역 · 기능 번호가 빠짐없이 이어짐, 가리킨 번호가 실제로 있음. `tsc -b` · 새 파일 eslint 통과. `npm test` 907 통과 · 2 실패(레이아웃 플랜에 적힌 것과 같은 둘: `preset-candidate-source` digest, `vertical-vowel-gap-reference` HTML 해시 — 이 변경과 무관). e2e `dev-screen-spec.spec.ts` 3개 통과. 스크린샷으로 넓은 · 좁은 화면 확인. `vite build` 산출물에 버튼 · 명세 글자 없음(grep 0건).
- 사실(낡음): G0에 쓴 `자소편집 6`이 하루도 안 돼 낡았다. 다른 세션 커밋 둘(`39b698c` 부품 탭 → 캔버스 상자 누르기, `26348ca` 홀자 상자 네 변 rail)이 내가 읽은 뒤에 들어왔다. 6.1 · 6.2 · 6.6을 고쳤다. 주소 1:1 테스트는 이런 내용 낡음을 못 잡는다.
- 쓰다가 발견한 것(화면은 안 고침): ① 뼈대 · 자소 원형 · 조합별 결과에서 고친 값은 형태 저장소에만 남고 앱 글자 · 폰트 추출 어디에도 안 닿는다. 세 화면이 기록도 같이 쓴다. ② 조합별 결과의 비교 카드는 앱의 지금 획을 그려서 그 화면에서 한 보정이 카드에 안 보인다. 보정 모드를 끄는 버튼도 없다. ③ 자소 현황은 탭에서 갈 길이 없다. ④ 자소 원형 · 조합별 결과는 ㄱ 고정. ⑤ 자소 원형의 `결과 윤곽` 버튼은 눌린 모양뿐 동작이 없고, 면 채우기가 꺼질 때 사유가 안 나온다.
- 가설: 세 화면(`자소현황` `자소원형` `조합별결과`)의 `status`를 `실험`으로 적었다. 사용자가 정한 게 아니라 "글자에 안 닿음 · ㄱ 고정 · 화면 검토 모드"라는 코드 상태를 보고 붙인 것. G3에서 확인.
- 미결정: ① 명세가 낡지 않게 하는 규칙을 `CLAUDE.md` · `AGENTS.md`에 넣을지("화면을 고치면 같은 커밋에서 `docs/specs/화면/`도 고친다"). 플랜 5번이지만 규칙 문서라 승인 없이는 안 고쳤다. ② 위 `실험` 표시. ③ G3 — 사용자가 트리만 보고 기능 셋을 번호로 짚어 대화가 되는지, 개발 화면에서 버튼으로 같은 내용이 뜨는지.

**2026-09-21 G2 e2e 회귀 확인 · G2 닫힘**
- 사실(회귀 1건, 고침): 좁은 화면 버튼을 16×44 탭으로 만들었더니 `shape-workspace-shell`의 셸 계약("보이는 버튼 · 링크는 44×44 이상") 테스트 8개가 깨졌다. 테스트는 안 고치고 버튼의 터치 영역을 44×44로 맞췄다(보이는 탭만 얇게, 패널 닫기 · 목록 링크도 44). 고친 뒤 8개 통과.
- 사실(기준선 비교): 전체 e2e — 버튼 켬 91 통과 · 29 실패, 버튼 끔(`VITE_SCREEN_SPEC=off`, 새 서버) 90 통과 · 28 실패(내 e2e 3개 제외). 실패 목록의 차이는 셋뿐: `shape-workspace-shell` 두 개는 돌릴 때마다 다른 테스트가 하나씩 떨어지는 불안정(버튼 꺼도 하나 실패), `design-body-internal-spacing:3`은 늘 떠 있던 4173 서버에서 버튼을 꺼도 4/4 실패. 즉 남은 실패는 전부 이 변경 이전부터 있던 것이다. 파일별: `font-guide-lab` 10, `mobile-editor-v2` 9, `mobile-editor-v2-contexts` 2, OTF 기준선 2, `design-body` 2, `noto-corpus-lab` · `calibration-layout-persistence` · `area-stroke-style` 각 1.
- 사실(원인 하나): `design-body-internal-spacing`의 실패 값은 .244 × 850/600 = .346과 정확히 맞는다. 글자 폭은 네모꼴을 따라 줄었는데 글자 모양은 안 줄었다는 뜻 — 모델 상자로 그려지는 글자는 네모꼴을 안 따른다. 새 서버에서 통과한 건 모델이 늦게 와서 재는 순간 옛 스키마로 그려졌기 때문이다(모델 로드와의 경주). 명세를 고쳤다: `자소편집 8.1`과 `_트리`의 저장소 표에 "글자 폭에는 닿고 모양에는 안 닿음".
- 사실(다른 세션): 그 사이 뼈대 화면이 제거됐고 같은 작업에서 `뼈대.md` 삭제, `screenRoutes` · `_트리` · 다른 명세의 가리킨 줄까지 정리돼 있었다. 주소 1:1 테스트가 제 역할을 한 셈. 빈틈 하나를 메웠다 — 없어진 명세 이름을 계속 가리켜도 테스트가 못 잡던 것(모르는 이름은 무시)을 `이름 숫자` 꼴이면 아는 이름이어야 한다는 검사로 추가, `_트리.md`도 같이 본다.
- 사실(추가): 버튼을 끄는 스위치 `VITE_SCREEN_SPEC=off`를 `main.tsx` 분기에 넣었다(기준선 비교용으로 만들었고 그대로 둠).
- 사실(최종): `screen-spec.test.ts` 15개, `tsc -b`, 새 파일 eslint 통과. 제품 화면 6개 + 랩 1개 명세, `_트리.md`.
- 미결정(G3에서): ① 명세 갱신 규칙을 `CLAUDE.md` · `AGENTS.md`에 넣을지 ② `자소현황` `자소원형` `조합별결과`의 `실험` 표시 ③ 기존 e2e 실패 28개는 이 플랜 밖 — 따로 정리할지.
